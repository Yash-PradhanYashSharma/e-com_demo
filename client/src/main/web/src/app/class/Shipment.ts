export class Shipment {
  width: number;
  depth: number;
  height: number;
  weight: number;

  constructor(width: number, depth: number, height: number, weight: number) {
    this.width = width;
    this.depth = depth;
    this.height = height;
    this.weight = weight;
  }
}
